# 📚 EasyCheck System Documentation

## 🎯 ภาพรวมระบบ

ระบบ EasyCheck เป็นระบบบริหารจัดการการเช็คอินและกิจกรรมของพนักงาน โดยใช้ตำแหน่งทางภูมิศาสตร์ (Geolocation) ในการยืนยันตัวตน

---

## 🗺️ การจัดการพื้นที่และกิจกรรม (Mapping & Events)

### 📍 พื้นที่อนุญาต (Locations)
พื้นที่ที่กำหนดให้พนักงานสามารถเช็คอินได้

**คุณสมบัติ:**
- ✅ กำหนดตำแหน่ง GPS (Latitude, Longitude)
- ✅ กำหนดรัศมีพื้นที่ (เมตร)
- ✅ ตั้งค่าชื่อและรายละเอียดสถานที่
- ✅ เปิด/ปิดการใช้งาน (Active/Inactive)
- ✅ แสดงบนแผนที่ด้วยหมุดสีเขียว 🟢

### 📅 กิจกรรม (Events)
กิจกรรมพิเศษที่กำหนดวันเวลาและทีมที่เข้าร่วม

**คุณสมบัติ:**
- ✅ กำหนดชื่อ วันที่ และเวลากิจกรรม
- ✅ กำหนดตำแหน่งและรัศมี
- ✅ ระบุทีมที่เข้าร่วม (Team-based filtering)
- ✅ สถานะ: เริ่มงานแล้ว/เสร็จสิ้น
- ✅ แสดงบนแผนที่ด้วยหมุดสีน้ำเงิน 🔵

---

## 🔄 การซิงค์ข้อมูลระหว่าง Admin และ User

### 🎯 กลไกการทำงาน

#### 1. **EventContext (Centralized State Management)**
```javascript
// Location: src/contexts/EventContext.jsx

// ฟังก์ชันสำคัญ:
- getEventsForUser(userDepartment, userPosition)
  → กรองกิจกรรมตามแผนก/ตำแหน่งของผู้ใช้

- canJoinEvent(event)
  → ตรวจสอบว่าผู้ใช้สามารถเข้าร่วมได้หรือไม่
  → เงื่อนไข: ภายใน 30 นาทีหลังเวลาเริ่มกิจกรรม

- getTimeRemainingToJoin(event)
  → คำนวณเวลาที่เหลือก่อนปิดรับเข้าร่วม
```

#### 2. **Data Flow**

```
┌─────────────────────────────────────────────────────────┐
│  Admin: MappingAndEvents.jsx                            │
│  - สร้าง/แก้ไข/ลบ กิจกรรม                              │
│  - กำหนด teams: ['ทีมขาย', 'Marketing', 'IT']          │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ Save to localStorage
                     ↓
┌─────────────────────────────────────────────────────────┐
│  EventContext (Global State)                            │
│  - localStorage: 'easycheck_events'                     │
│  - ซิงค์ข้อมูลอัตโนมัติทุกครั้งที่มีการเปลี่ยนแปลง    │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ Filter by teams
                     ↓
┌─────────────────────────────────────────────────────────┐
│  User: EventList.jsx                                    │
│  const userEvents = getEventsForUser(                   │
│    user.department,  // เช่น 'ฝ่ายขาย'                 │
│    user.position     // เช่น 'พนักงานขาย'              │
│  )                                                       │
│                                                          │
│  → แสดงเฉพาะกิจกรรมที่ user มีสิทธิ์เข้าร่วม           │
└─────────────────────────────────────────────────────────┘
```

#### 3. **Team Matching Logic**

```javascript
// การกรองกิจกรรมตามทีม
getEventsForUser(userDepartment, userPosition) {
  const userTeams = [userDepartment, userPosition]
    .filter(Boolean)
    .map(t => t.toLowerCase())
  
  return events.filter(event => {
    // ถ้ากิจกรรมไม่ระบุทีม = เปิดให้ทุกคนเข้าร่วม
    if (!event.teams || event.teams.length === 0) {
      return true
    }
    
    // ตรวจสอบว่า user แมทช์กับทีมใดทีมหนึ่งหรือไม่
    return event.teams.some(eventTeam => 
      userTeams.some(userTeam => 
        eventTeam.toLowerCase().includes(userTeam) || 
        userTeam.includes(eventTeam.toLowerCase())
      )
    )
  })
}
```

**ตัวอย่างการแมทช์:**
```
User: { department: "ฝ่ายขาย", position: "พนักงานขาย" }
Event: { teams: ["ทีมขาย", "Marketing"] }

✅ MATCH! 
- "ฝ่ายขาย".includes("ขาย") 
- "ทีมขาย".includes("ขาย")
```

---

## 🔐 การเช็คอิน (Check-in System)

### เงื่อนไขการเช็คอิน

#### 1. **Location Verification** 🌍
```javascript
// User ต้องอยู่ภายในรัศมีที่กำหนด
const distance = calculateDistance(
  userLatitude, userLongitude,
  eventLatitude, eventLongitude
)

if (distance <= event.radius) {
  // ✅ อนุญาตให้เช็คอิน
} else {
  // ❌ แจ้งเตือน "คุณอยู่นอกพื้นที่"
}
```

#### 2. **Time Window Verification** ⏰
```javascript
// กรณีกิจกรรมปกติ
- สามารถเช็คอินได้ตามเวลาทำงานปกติ

// กรณีกิจกรรมพิเศษ (Events)
- เช็คอินได้ภายใน 30 นาทีหลังเวลาเริ่มกิจกรรม
- ตัวอย่าง: กิจกรรมเริ่ม 14:00 → เช็คอินได้ถึง 14:30

const canJoinEvent = (event) => {
  const eventStartTime = parseEventDateTime(event.date, event.startTime)
  const joinDeadline = eventStartTime + 30 minutes
  const now = new Date()
  
  return now <= joinDeadline
}
```

#### 3. **Team Permission** 👥
```javascript
// User เห็นเฉพาะกิจกรรมที่ตัวเองมีสิทธิ์เข้าร่วม
const visibleEvents = getEventsForUser(
  user.department, 
  user.position
)

// ถ้าไม่อยู่ในรายชื่อทีม = ไม่เห็นกิจกรรม
```

---

## 🎨 UI/UX Features

### 1. **หน้า Admin: MappingAndEvents.jsx**

#### แผนที่รวม (Combined Map)
- 🟢 **หมุดสีเขียว** = พื้นที่อนุญาตปกติ
- 🔵 **หมุดสีน้ำเงิน** = กิจกรรม
- 📊 **Legend** = แสดงจำนวนพื้นที่และกิจกรรมทั้งหมด
- 🔍 **Auto-fit bounds** = ขยายแผนที่ให้เห็นหมุดทั้งหมด

#### Tab Navigation
- **Tab "พื้นที่อนุญาต"** 🟢
  - จัดการพื้นที่เช็คอินปกติ
  - เพิ่ม/แก้ไข/ลบ พื้นที่
  - รายการแสดงแบบ Scrollable (max 3 items visible)

- **Tab "กิจกรรม"** 🔵
  - จัดการกิจกรรมพิเศษ
  - Time Picker แบบ Dropdown (HH:MM)
  - ระบุทีมที่เข้าร่วม
  - รายการแสดงแบบ Scrollable

#### การคลิก "ดูรายละเอียด" จาก Popup
```javascript
// เมื่อคลิกปุ่มในแผนที่:
1. สลับไปยัง Tab ที่เกี่ยวข้อง (พื้นที่/กิจกรรม)
2. Scroll ไปยังรายการที่เลือก
3. Highlight ด้วย ring effect (2 วินาที)
```

### 2. **หน้า User: EventList.jsx**

#### การแสดงผล
- แสดงเฉพาะกิจกรรมที่ user มีสิทธิ์เข้าร่วม
- เรียงลำดับ: กิจกรรมที่ดำเนินการอยู่ (ongoing) ขึ้นก่อน
- แสดงข้อมูล:
  - 📍 สถานที่และพิกัด
  - ⏰ เวลาเริ่มต้น-สิ้นสุด
  - 👥 ทีมที่เข้าร่วม
  - 🎯 รัศมีพื้นที่

#### EventDetails.jsx
- แผนที่แสดงตำแหน่งกิจกรรม
- ปุ่ม "เข้าร่วมกิจกรรม"
  - เปิดใช้งานเมื่อ: อยู่ในรัศมี + ภายในเวลา 30 นาที
  - ปิดใช้งานเมื่อ: หมดเวลาหรืออยู่นอกพื้นที่
- แสดง Countdown เวลาที่เหลือ

---

## 📦 Data Structure

### Location Schema
```javascript
{
  id: number,
  name: string,           // ชื่อสถานที่
  description: string,    // รายละเอียด
  latitude: number,       // พิกัด latitude
  longitude: number,      // พิกัด longitude
  radius: number,         // รัศมี (เมตร)
  status: 'active' | 'inactive'  // สถานะ
}
```

### Event Schema
```javascript
{
  id: number,
  name: string,           // ชื่อกิจกรรม
  date: string,           // วันที่ (DD/MM/YYYY)
  description: string,    // รายละเอียด
  locationName: string,   // ชื่อสถานที่
  latitude: number,       // พิกัด latitude
  longitude: number,      // พิกัด longitude
  radius: number,         // รัศมี (เมตร)
  status: 'ongoing' | 'completed',  // สถานะ
  startTime: string,      // เวลาเริ่ม (HH:MM)
  endTime: string,        // เวลาสิ้นสุด (HH:MM)
  teams: string[]         // ทีมที่เข้าร่วม ['ทีมขาย', 'IT', ...]
}
```

---

## 🔧 Technical Implementation

### Components Architecture

```
src/
├── pages/
│   ├── admin/
│   │   ├── MappingAndEvents.jsx     # หน้ารวมพื้นที่และกิจกรรม
│   │   ├── Mapping.jsx              # Component จัดการพื้นที่
│   │   └── EventManagement.jsx      # Component จัดการกิจกรรม
│   └── user/
│       └── Event/
│           ├── EventList.jsx         # รายการกิจกรรมของ user
│           └── EventDetails.jsx      # รายละเอียดกิจกรรม
│
├── contexts/
│   ├── EventContext.jsx             # Global state management
│   ├── LocationContext.jsx          # Location management
│   └── AuthContext.jsx              # User authentication
│
└── main.jsx                         # Routing configuration
```

### Key Props & State

#### MappingAndEvents.jsx
```javascript
// State
const [activeTab, setActiveTab] = useState('locations')
const [scrollToLocationId, setScrollToLocationId] = useState(null)
const [scrollToEventId, setScrollToEventId] = useState(null)

// Props ส่งให้ child components
<Mapping 
  hideHeader={true} 
  hideMap={true} 
  scrollToId={scrollToLocationId} 
/>

<EventManagement 
  hideHeader={true} 
  hideMap={true} 
  scrollToId={scrollToEventId} 
/>
```

#### Mapping.jsx & EventManagement.jsx
```javascript
// Receive props
function Mapping({ hideHeader = false, hideMap = false, scrollToId = null })

// Auto-scroll effect
useEffect(() => {
  if (scrollToId && refs.current[scrollToId]) {
    const element = refs.current[scrollToId]
    element.scrollIntoView({ behavior: 'smooth', block: 'center' })
    element.classList.add('ring-4', 'ring-green-400')
    setTimeout(() => {
      element.classList.remove('ring-4', 'ring-green-400')
    }, 2000)
  }
}, [scrollToId])
```

---

## 🎯 User Flow Examples

### ตัวอย่างที่ 1: Admin สร้างกิจกรรมใหม่
```
1. Admin เข้าหน้า "ตั้งค่าพื้นที่และกิจกรรม"
2. เลือก Tab "กิจกรรม"
3. คลิก "เพิ่มกิจกรรมใหม่"
4. กรอกข้อมูล:
   - ชื่อ: "Workshop การพัฒนาระบบ"
   - วันที่: 21/10/2025
   - เวลา: 09:00 - 17:00 (ใช้ Time Picker)
   - ทีม: IT, ทีมพัฒนา, Software Engineer
   - คลิกบนแผนที่เพื่อเลือกตำแหน่ง
5. คลิก "ยืนยัน"
6. ✅ กิจกรรมถูกบันทึกลง EventContext
7. 🔄 ซิงค์ไปยัง localStorage อัตโนมัติ
```

### ตัวอย่างที่ 2: User เห็นกิจกรรมและเช็คอิน
```
1. User (แผนก: IT, ตำแหน่ง: Software Engineer) เข้าระบบ
2. ไปที่หน้า "กิจกรรม"
3. เห็นกิจกรรม "Workshop การพัฒนาระบบ"
   ✅ เพราะอยู่ในทีม "Software Engineer"
4. คลิกดูรายละเอียด
5. ระบบตรวจสอบ:
   ✅ อยู่ในรัศมีพื้นที่ (GPS)
   ✅ อยู่ในช่วงเวลา (09:00 - 09:30)
6. แสดงปุ่ม "เข้าร่วมกิจกรรม" (เปิดใช้งาน)
7. User คลิกเข้าร่วม
8. ✅ เช็คอินสำเร็จ
```

### ตัวอย่างที่ 3: User ไม่เห็นกิจกรรม
```
User (แผนก: ฝ่ายการเงิน, ตำแหน่ง: บัญชี)
→ ไม่เห็นกิจกรรม "Workshop การพัฒนาระบบ"
❌ เพราะไม่ได้อยู่ในทีม ['IT', 'ทีมพัฒนา', 'Software Engineer']

→ เห็นเฉพาะกิจกรรมที่:
  - ไม่ระบุทีม (เปิดให้ทุกคนเข้าร่วม)
  - หรือระบุทีมที่ตรงกับ "การเงิน" หรือ "บัญชี"
```

---

## ⚠️ Important Notes

### 1. localStorage Persistence
- ข้อมูลทั้งหมดเก็บใน localStorage
- Key: `'easycheck_events'`, `'easycheck_locations'`
- Auto-save ทุกครั้งที่มีการเปลี่ยนแปลง

### 2. GPS Accuracy
- ใช้ Geolocation API
- ต้องอนุญาตการเข้าถึงตำแหน่งบนเบราว์เซอร์
- Accuracy ขึ้นอยู่กับอุปกรณ์และสัญญาณ GPS

### 3. Time Zone
- ใช้เวลาท้องถิ่นของอุปกรณ์
- ต้องแน่ใจว่าเวลาในอุปกรณ์ถูกต้อง

### 4. Team Matching
- Case-insensitive (ไม่สนใจตัวพิมพ์เล็ก/ใหญ่)
- Partial match (แมทช์บางส่วนได้)
- ตัวอย่าง: "ทีมขาย" จะแมทช์กับ "ฝ่ายขาย", "พนักงานขาย"

---

## 🚀 Future Enhancements

### Planned Features
- [ ] Push notification เตือนก่อนกิจกรรมเริ่ม
- [ ] Export รายงานการเข้าร่วมกิจกรรม
- [ ] QR Code check-in
- [ ] รองรับ multiple locations ต่อกิจกรรม
- [ ] Calendar view สำหรับกิจกรรม
- [ ] Team management interface

---

## 📞 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Console log ในเบราว์เซอร์
2. ตรวจสอบ localStorage data
3. ตรวจสอบการอนุญาตการเข้าถึง GPS
4. Refresh หน้าเพื่อ reload state

---

## 🎓 Summary

ระบบ EasyCheck ใช้การ**ซิงค์ข้อมูลแบบ Real-time ผ่าน Context API และ localStorage** ทำให้:
- ✅ Admin จัดการพื้นที่และกิจกรรมได้ง่าย
- ✅ User เห็นเฉพาะกิจกรรมที่เกี่ยวข้อง
- ✅ การเช็คอินมีความปลอดภัย (ตรวจสอบ GPS + เวลา)
- ✅ ข้อมูลไม่สูญหายแม้ปิดเบราว์เซอร์
- ✅ Scroll to detail ทำงานได้อัตโนมัติเมื่อคลิกจากแผนที่

**Last Updated:** October 24, 2025
